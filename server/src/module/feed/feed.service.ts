import { Injectable } from '@nestjs/common';
import { Board } from '../board/board.entity';

@Injectable()
export class FeedService {
    //async getAllFeed(board: Board): Promise<Board[]> {
    //     return this.boards
    // }
}
